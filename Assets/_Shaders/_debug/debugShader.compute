#pragma kernel CSMain

RWStructuredBuffer<uint> _buffer; // size = THREADS_PER_BLOCK * BLOCK_SIZE

[numthreads(128,1,1)]
void CSMain (uint3 id : SV_DispatchThreadID)
{
    const uint threadId = id.x;
    _buffer[threadId] = firstbithigh(5);
}
